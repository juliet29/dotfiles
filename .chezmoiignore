# Patterns are matched against TARGET paths (relative to $HOME), not source names.
# Ignored entries are not managed/applied by chezmoi and won't be re-added.


# # tmux plugins are installed manually via git
# .config/tmux/plugins
